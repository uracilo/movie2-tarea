<%= form_for(animale) do |f| %>
  <% if animale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animale.errors.count, "error") %> prohibited this animale from being saved:</h2>

      <ul>
      <% animale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :scientificname %>
    <%= f.text_field :scientificname %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
